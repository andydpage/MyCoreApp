kind: pipeline
type: exec
name: MyCoreApp

platform:
  os: windows
  arch: amd64

steps:
- name: greeting
  commands:
  - echo hello world 3
- name: build
  commands:
#  - pwd
#  - "dir env:"
#  - $env:Path += ';C:\Users\andrew_page\AppData\Local\Microsoft\dotnet'
#  - $env:path
#  - "dir env:"
#  - dotnet --list-sdks
   - $env:NUGET_PACKAGES = 'C:\Users\andrew_page\.nuget\packages'
   - dotnet build
   - dotnet publish --configuration Release
   - Compress-Archive -Path $env:DRONE_WORKSPACE\MyCoreApp\bin\Release\netcoreapp3.1 -DestinationPath C:\Windows\Temp\mycoreapp.zip
#   - sleep 120
