kind: pipeline
type: exec
name: MyCoreApp

platform:
  os: windows
  arch: amd64

steps:
- name: greeting
  commands:
  - echo hello world 3
- name: build
  commands:
  - pwd
  - Get-ChildItem -Path .\*
#  - $env:Path += ';C:\Users\andrew_page\AppData\Local\Microsoft\dotnet'
#  - $env:path
#  - "dir env:"
#  - dotnet --list-sdks
#  - dotnet restore

